version: 2

sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        columns:
          - name: payment_method
            description: '{{ doc("payment_method")}}'